from django.urls import path

# Skeleton URLconf for the "evento" module.
# Namespace is registered so {% url 'evento:...' %} references resolve once
# the corresponding views and routes are implemented.
app_name = "evento"

urlpatterns = [
]
