from django.urls import path

# Skeleton URLconf for the "saude" module.
# Namespace is registered so {% url 'saude:...' %} references resolve once
# the corresponding views and routes are implemented.
app_name = "saude"

urlpatterns = [
]
