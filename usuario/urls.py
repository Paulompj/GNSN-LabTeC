from django.urls import path

# Skeleton URLconf for the "usuario" module.
# Namespace is registered so {% url 'usuario:...' %} references resolve once
# the corresponding views and routes are implemented.
app_name = "usuario"

urlpatterns = [
]
