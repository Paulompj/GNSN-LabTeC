from django.urls import path

# Skeleton URLconf for the "cirio" module.
# Namespace is registered so {% url 'cirio:...' %} references resolve once
# the corresponding views and routes are implemented.
app_name = "cirio"

urlpatterns = [
]
