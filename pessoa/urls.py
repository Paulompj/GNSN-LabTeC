from django.urls import path

# Skeleton URLconf for the "pessoa" module.
# Namespace is registered so {% url 'pessoa:...' %} references resolve once
# the corresponding views and routes are implemented.
app_name = "pessoa"

urlpatterns = [
]
